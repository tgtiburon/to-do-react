/*function hilightCode() {
    hljs.highlightAll();

    try {
        let body = document.querySelector('code');
        let highlightedBody = hljs.highlightAuto(body.value);

        body.value = highlightedBody.value;
    } catch (error) {
        console.log(error, " code was not found");
    }
}

export { hilightCode };*/